# Node
node_modules/
app/.next/
app/out/
app/.env*

# Terraform — provider binaries and state (never commit state or secrets)
**/.terraform/
**/*.tfstate
**/*.tfstate.backup
**/tfplan
terraform/terraform.tfvars

# Packer logs and cache
packer/*.log
packer/packer_cache/

# OS
.DS_Store
Thumbs.db

# Secrets
*.pem
*.key
.env

# Large files
*.png.bak
